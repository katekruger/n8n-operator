"""The default-deny workflow registry (ADR-002).

The registry is the allowlist: a workflow absent from it is invisible and unexecutable,
however live it is on the n8n instance. Normative schema in ``docs/BUILD_PLAN.md``
section 6; authoring guide in ``docs/WORKFLOW_REGISTRY.md``.
"""
