"""Contract tests (BUILD_PLAN section 10.3).

Tool inventory and schemas, the error taxonomy, the response-shaping allowlist, the
layering rule (ADR-001), the append-only audit rule (B11), the no-retry rule (ADR-005),
and documentation consistency.
"""
