# Fixture: violates rule R9 only. Used by tests/unit/test_loader_rules.py.
apiVersion: n8n-operator/v1
metadata:
  name: fixture
  description: A single-rule-violation fixture.
workflows:
- id: fixture.workflow
  n8n_workflow_id: n8n-abc-123
  title: Fixture workflow
  description: Exists only to exercise one load-time rule violation.
  owner: test-owner
  version: 1
  definition_hash: sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
  risk: low
  side_effects: read_only
  approval: none
  trigger:
    type: webhook
    method: POST
    path: /webhook/fixture
    auth: none
  input_schema:
    type: object
    additionalProperties: false
    properties:
      x:
        type: string
  output:
    redact:
    - not a valid jsonpath(((
